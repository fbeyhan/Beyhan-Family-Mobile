// Firestore data structure reference for your app
// This file documents the collections and example documents for development

// users/{userId}
//   { role: 'admin' | 'member', name: string, email: string, ... }

// family/{docId}
//   { groupName: string, description: string, ... }

// familyMembers/{memberId}
//   { firstName: string, lastName: string, dob: string, ... }

// finance/{docId}
//   { type: 'expense' | 'income', amount: number, date: string, ... }

// assets/{assetId}
//   { name: string, balance: number, ... }

// This file is for developer reference only and not imported in the app.
